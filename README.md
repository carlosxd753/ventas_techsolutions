Pasos para poder correr el proyecto es necesario tener instalado docker, Java 21 y Maven
Los pasos para poder correr la aplicacion son las siguientes:
1) mvn clean package -DskipTests
2) docker compose up
